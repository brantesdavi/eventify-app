import { TimepickerModule } from './timepicker.module';

describe('TimepickerModule', () => {
  const module: TimepickerModule = new TimepickerModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
