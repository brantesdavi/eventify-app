import { NewEventComponent } from './new-event.component';

describe('NewEventComponent', () => {
  let component: NewEventComponent;

  beforeEach(() => {
    component = new NewEventComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
